#include "main.h"
/**
 * main - check the code
 *
 * print_alphabet - function prints alphabet in lower case
 * Return: Always 0.
 */



void print_alphabet(void)
{
	char u;
	for (u = 'a' ; u <= 'z' ; u++)
		_putchar(u);
	_putchar('\n');
}
